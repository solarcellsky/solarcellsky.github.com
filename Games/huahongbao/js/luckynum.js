var serverLuckNum =0;
var fx_domains = ["59ishopping.cn"]; 
var cur_date = Date.now(); 
var cur_fx_domain = "mp"+parseInt(cur_date)+"wx."+fx_domains[cur_date%fx_domains.length]; if(cur_date%50==1) { 
	
}